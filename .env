# Source from the repository root: source .env
# Worker secrets and local runtime settings belong in .dev.vars.

setup() {
  pnpm install --frozen-lockfile && pnpm db:migrate
}

build() {
  pnpm build
}

run() {
  ./run.sh
}

test() {
  pnpm test "$@"
}
